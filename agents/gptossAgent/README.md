# GPT-OSS Agent Notes

Operational guidance moved to `.agents/skills/agent-scaffolds/`.

Use scaffold `gptossbasic` or `gptossallq` explicitly. The skill also carries the current GPT-OSS launch knobs such as `gptoss_reasoning_effort` and large `agent_max_model_len` settings.
